## Configuration file fields

used for naming plots when saved in validation and training scripts

## Configuration file sub-fields

- `label`: label to use for axis labeling
- `loc`: index of the kinematic variable
- `min`: lower bound of binning
- `max`: upper bound of binning
- `nbins`: number of bins to use in histograms