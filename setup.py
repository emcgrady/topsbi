import setuptools

setuptools.setup(
    name='topsbi',
    version='0.0.0',
    description='Framework and tools to build and validate networks used for likelihood-free inference',
    packages=setuptools.find_packages()
)